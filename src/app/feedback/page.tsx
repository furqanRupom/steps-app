import * as React from 'react';

interface IFeedbackPageProps {
}

const FeedbackPage: React.FunctionComponent<IFeedbackPageProps> = (props) => {
    return <section className='min-h-screen  flex items-center justify-center w-full h-full bg-gradient-to-t from-black to-gray-900 backdrop-blur-2xl text-center'>

        <h3>client feedback</h3>

  </section>;
};

export default FeedbackPage;
